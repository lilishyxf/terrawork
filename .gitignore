# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 虚拟环境（架构基线规定解释器：Python 3.11.x）
.venv/

# IDE / 编辑器
.idea/
.vscode/

# Claude Code 本机配置（per-developer 权限允许表，不入库）
.claude/settings.local.json

# LLM provider API key（ADR-009，禁止入库）
.env

# TerraWorks 运行数据（见 ARCHITECTURE 第 11 节：session.db 与 worktrees 不入库）
# 忽略所有 db，但 data/fixtures/ 下的版本化 fixture 除外
*.db
!data/fixtures/*.db
*.db-wal
*.db-shm
data/worktrees/
data/sandbox-repo/

# 前端(M3 View)
node_modules/
dist/
# Tauri 壳(待 Rust 就绪)
src-tauri/target/

# Vite Windows 临时配置文件
vite.config.ts.timestamp-*.mjs

# 精灵原图(白底,prep_sprites.py 的输入,不入库;产物 public/sprites|portraits 入库)
sprites_raw/
portraits_raw/

# 帧拼图(人工识别帧范围用,临时)
frame_montage_*.png

# 室内图原始备份(裁切前),不入库
public/rooms/*_raw.png
data/archive/

# Tauri sidecar 二进制(打包产物,勿入库)
src-tauri/binaries/
build/pyi-dist/
build/pyi-work/
build/*.spec

# 供应商配置(含 API key,勿入库)
data/providers.json

build/
