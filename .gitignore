# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 虚拟环境（架构基线规定解释器：Python 3.11.x）
.venv/

# IDE / 编辑器
.idea/
.vscode/

# Claude Code 本机配置（per-developer 权限允许表，不入库）
.claude/settings.local.json

# LLM provider API key（ADR-009，禁止入库）
.env

# TerraWorks 运行数据（见 ARCHITECTURE 第 11 节：session.db 与 worktrees 不入库）
# 忽略所有 db，但 data/fixtures/ 下的版本化 fixture 除外
*.db
!data/fixtures/*.db
*.db-wal
*.db-shm
data/worktrees/

# 前端(M3 View)
node_modules/
dist/
# Tauri 壳(待 Rust 就绪)
src-tauri/target/
